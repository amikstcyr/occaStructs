
#include "include/struct.h"

@kernel void structKernel(int N, struct_t *q){

  for(int b=0;b<(N+p_B-1)/p_B;++b;@outer(0)){
    for(int t=0;t<p_B;++t;@inner(0)){
      int n = t + b*p_B;
      if(n<N){
	q[n].t = t;
	q[n].b = b;
	q[n].val = n;
      }
    }
  }

}
